// 파이썬의 리스트, C언어의 배열과 유사
// 앞에서 봤던 data_type을 모두 사용할 수 있다.
const emptyArray = []; //빈 배열 생성

const arrayMembers = ["a", 2, true, 99.9, "멋사"];

console.log(arrayMembers);

console.log(arrayMembers[1]);
