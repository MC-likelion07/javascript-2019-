function getName(name) {
  console.log("내 이름은 " + name);
}

function getLeg(leg) {
  console.log(`내 다리의 개수는 ${leg}개얌!`);
}

const id_1 = getName("King"); // 함수 호출
const id_2 = getName("Queen");
const id_3 = getName("개미");

// const id_4 = getLeg(6);

// 결론 : 관련 있는 내용들을 묶기 위해서 class를 사용한다.
